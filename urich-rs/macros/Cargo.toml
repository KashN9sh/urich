[package]
name = "urich-rs-macros"
version = "0.1.0"
edition = "2021"
description = "Derive macros for urich-rs (Command, Query)"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
