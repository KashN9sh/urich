[workspace]
resolver = "2"
# urich-python is built with maturin (see urich-python/README); omit from default build to avoid link errors.
members = ["urich-core", "urich-rs"]
exclude = ["urich-python"]
