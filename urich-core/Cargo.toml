[package]
name = "urich-core"
version = "0.1.0"
edition = "2021"
description = "Urich core: HTTP, routing, validation, host callback"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
tiny_http = "0.12"
